Steps for Running Code:

1. Clone the repository
2. Run "main.cpp"

SFML dlls are included in the repository, so it shouldn't require a local installation of SFML.

* Priorities are on a 1-5 scale with 1 being the lowest priority and 5 being the highest.
* states are entered in as capatalized initials (Florida = FL).